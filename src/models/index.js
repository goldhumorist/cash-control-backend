const User = require('./User');
const Category = require('./Category');
const Purchase = require('./Purchase');
const BankClient = require('./Fake-bank-client');

module.exports = {
  User,
  Category,
  Purchase,
  BankClient,
};
